# ========================================
# MoonDev AI Trading Agents — Dependencies
# Optimized for Windows + Forex/XAU
# ========================================

# Web serving (frontend dashboard)
fastapi==0.109.0
uvicorn==0.25.0
python-multipart==0.0.6
jinja2==3.1.2

# Core
python-dotenv==1.0.0
requests==2.31.0
PyPDF2==3.0.0
pandas>=2.1.0
numpy>=1.24.0
termcolor==2.3.0
schedule==1.2.0
python-dateutil==2.8.2
backoff==2.2.1

# AI APIs
openai>=1.59.5       # Used for DeepSeek (OpenAI-compatible) + ChatGPT
google-generativeai>=0.8.0  # Gemini fallback

# YouTube transcripts (rbi_agent)
youtube-transcript-api==0.6.2

# Technical Analysis (pandas-ta: Windows-friendly, no binary needed)
# NOTE: TA-Lib requires separate binary on Windows, use pandas-ta instead
pandas-ta==0.3.14b0

# Backtesting framework
backtesting>=0.3.3

# Market Data (yfinance for XAUUSD, BTCUSD, FX pairs)
yfinance>=0.2.36

# Optional: for chart agent
# Pillow>=10.0.0
